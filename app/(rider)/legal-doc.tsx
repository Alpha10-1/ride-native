// Shared implementation lives in src/screens/LegalDocScreen.tsx — takes a
// `docKey` param matching a row in app_content (e.g. "privacy_policy").
export { default } from "../../src/screens/LegalDocScreen";